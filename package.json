{
  "name": "eva-dutch-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Lingua — profesor de holandés por Telegram (bot): intents en español, sesiones de repaso con botones SM-2, recordatorios cada 4h y API de ping. Consume eva-dutch-service (3022).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "npm run build && node --test \"dist/tests/*.test.js\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.14",
    "typescript": "^5.8.3"
  }
}
